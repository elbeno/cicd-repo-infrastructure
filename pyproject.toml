[project]
name = "intel-cicd-repo-infrastructure"
version = "0.0.1"
requires-python = ">=3.12"
dependencies = [
  "pytest",
  "pytest-xdist",
  "hypothesis",
  "black",
  "mypy",
  "cmakelang",
  "pyyaml",
  "clang-format"
]
